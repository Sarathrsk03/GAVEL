
export const API_BASE_URL_GATEWAY = 'http://localhost:8000';

export const ENDPOINTS = {
    SUMMARIZE: `${API_BASE_URL_GATEWAY}/summarize`,
    VERIFY: `${API_BASE_URL_GATEWAY}/verify`,
    SEARCH: `${API_BASE_URL_GATEWAY}/search`,
    DRAFT: `${API_BASE_URL_GATEWAY}/draft`,
};
